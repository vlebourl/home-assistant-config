platform: template
sensors:
  home:
    friendly_name: Home presence
    value_template: >
      {% if is_state('person.virginia', 'A la maison') and
         is_state('person.vincent', 'A la maison') %}
        both
      {% elif is_state('person.virginia', 'A la maison') %}
        Virginia
      {% elif is_state('person.vincent', 'A la maison') %}
        Vincent
      {% else %}
        empty
      {% endif %}
