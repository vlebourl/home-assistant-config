platform: template
sensors:
  button_lock_button_used:
    value_template: >-
      {% if states.remote.interrupteur_entree.state == "right_upper_release" %}
        unlock
      {% elif states.remote.interrupteur_entree.state == "right_lower_release" %}
        lock
      {% else %}
        unknown
      {% endif %}

#      {{ (as_timestamp(utcnow()) - as_timestamp(strptime(states.sensor.button_lock_last_used_date.state,"%Y-%m-%d %H:%M:%S"))) | int }}
